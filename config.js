/* Generated at deploy from AUDIT_CLIENT_ID secret — not committed.
   Forks: copy config.js.example to config.js and fill your own client ID.
   For local file:// preview, a placeholder keeps the UI in setup state. */
window.WANTNOT_AUDIT_CLIENT_ID = '';
